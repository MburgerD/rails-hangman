<% if object %>
  <% if object.errors.any? %>
    <div id="error_explanation">
      <%= pluralize(object.errors.count, "error") %> prohibited this game from being saved

      <ul>
      <% object.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<% end %>
